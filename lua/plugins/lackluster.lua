return {
  "slugbyte/lackluster.nvim",
  lazy = false,
  priority = 1000,
  config = function()
    -- Optional: Configure lackluster here if needed
    -- require("lackluster").setup({
    --   -- your config options
    -- })
  end,
}

